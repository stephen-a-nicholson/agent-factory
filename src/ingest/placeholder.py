"""Placeholder ingest job entry point.

Replaced in phase 1 by a spark_python_task that calls into
src/ingest/structured.py. Kept thin on purpose: see CLAUDE.md, "Keep
notebooks thin."
"""

print("agent-factory: ingest job placeholder, no structured load implemented yet")
